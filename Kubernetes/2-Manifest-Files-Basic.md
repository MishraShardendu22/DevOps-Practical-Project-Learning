namespace 
daemon set/replica set / duplicaiton set / deployment 

label and selector 
scale get 5 replcias get 1 replica 

deployment rolling updates 

cron jobs and jobs 

adding volume and netowrk to linux 

showing deployements to teh outter world 
port fowrwarding (sudo -E)

Project deploy an app (in that image that top box is representing name space )
docker - docker hub login - push the latest docker image to docker hub - make a k8s folder - create a deployment yaml file - create a namespace yaml file - make a service yaml file - apply naemspace deployment service yaml files - expose the service to the outter world - port forwarding to the service - 